# Flask
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=cambia-esto-en-produccion-minimo-32-caracteres

# Base de datos (SQLite por defecto si se omite)
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/youtube_automator

# Cifrado de API keys y tokens (generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=

# Google OAuth (YouTube Data API)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://127.0.0.1:5000/youtube/oauth/callback

# Media generada
# MEDIA_ROOT=instance/media

# YouTube: si publish_mode=automatic en el perfil, visibilidad por defecto (review siempre private)
# YOUTUBE_DEFAULT_PRIVACY=private

# ElevenLabs: voz por defecto si no va voice_id en el JSON de credenciales
# ELEVENLABS_DEFAULT_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Rate limit (produccion: redis://localhost:6379)
# RATELIMIT_STORAGE_URI=memory://

# Logs JSON en stdout
# STRUCTLOG_JSON=true
# LOG_LEVEL=INFO
